# Project Status / Handoff

_Last updated: 2026-09-03 (ICT)_

This file is the canonical handoff for the current state of `machxanht/Ouk-Khmer-Online`. Read this before changing deployment, auth, ranked persistence, Vercel routing, or online gameplay.

## Repository safety

The repository is connected to Lovable. Do not rewrite published Git history. In particular: no force push, rebase, amend, or squash of commits that were already pushed. Use normal branches and merge commits.

`src/routeTree.gen.ts` is generated by TanStack Router and must not be edited manually.

## Current production architecture

- Frontend: Vercel project `ouk-khmer-online`.
- Stable Vercel URL: `https://ouk-khmer-online.vercel.app/`.
- Custom domain: `ouk.kuonkhmer.com` is attached to the correct Vercel project but is still blocked by registrar/DNSSEC state described below.
- Socket.IO backend: Railway service `ouk-khmer-backend`.
- Railway production host: `ouk-khmer-backend-production.up.railway.app`.
- Firebase project ID expected by backend auth: `project-by-khang`.

## Completed production configuration

Do not ask the user to repeat these unless there is evidence the configuration was removed.

- Railway has `FIREBASE_SERVICE_ACCOUNT_JSON` configured with the Firebase service-account JSON and the backend was redeployed successfully.
- Firestore hardening rules from `firestore.rules` were manually published in Firebase.
- Railway also has normal backend variables such as `NODE_ENV`, `PORT`, and `CORS_ORIGIN` configured.
- Vercel is linked to GitHub repo `machxanht/Ouk-Khmer-Online` and deploys `main` automatically.

## Security and ranked hardening already merged

### PR #1 — verified auth and server-authoritative rank

Merged to `main` with merge commit:

`d3abd0c366fdbba754a59013459854e408cfc872`

Implemented:

- Firebase ID tokens are cryptographically verified on the backend using Google Secure Token certificates.
- Production rejects development/test auth tokens.
- Matchmaking and private-room entry require verified Firebase identity.
- Public reconnect requires the server-issued match session token.
- Client-side Elo/stat writes were removed.
- Human-vs-human Elo/stat updates are calculated by the backend.
- Ranked updates plus immutable `match_history` creation are sent atomically through Firestore REST using backend service-account credentials.
- Ranked persistence is idempotent per game instance and duplicate history creation is protected with a Firestore create precondition.
- Bot fallback matches remain intentionally unranked.
- Firestore rules prevent clients from editing ranked fields.
- `.github/workflows/quality.yml` runs `npm ci`, `npm test`, `npm run test:auth`, `npm run check:assets`, and `npm run build`.

PR #1 Quality Gate and the post-merge `main` Quality Gate both passed.

### PR #2 — reconnect and PIN hardening

Merged to `main` with merge commit:

`5cc442c4d323051316fb272a70fa2ff39c7d61cd`

Implemented in `server/room-manager.ts`:

- Removed the lower-level reconnect-by-color fallback entirely.
- `handleReconnect` now requires a session token.
- Private-room PIN generation now uses `crypto.randomInt(100000, 1000000)` instead of `Math.random()`.
- Unique PIN generation fails explicitly after 100 unsuccessful attempts.

PR #2 Quality Gate and the post-merge `main` Quality Gate passed.

## Vercel production fixes already merged

### PR #3 — production output directory

Vite builds the frontend into `.output/public`. Vercel had previously been configured to serve the wrong directory, causing production 404s.

PR #3 changed Vercel output configuration to `.output/public` and was merged to `main` with commit:

`744ba00e3e783ee526e21ae9cc689f7eb4d2913f`

After this change the production Vercel deployment became reachable and returned HTTP 200.

### PR #4 — restore assets and sync AI check splash

Merged to `main` with merge commit:

`0dc7dda933b315f0d189101d6949dcebab10b4ad`

Problem found after PR #3:

- A catch-all Vercel rewrite sent requests such as `/assets/...` to `/index.html`.
- The page itself loaded, but bundled images such as the mascot/logo were broken because asset requests were rewritten to HTML.

PR #4 changed `vercel.json` from a catch-all SPA rewrite to explicit app-route rewrites (`/home`, `/play`, `/online`, `/leaderboard`, `/settings`, `/history`, `/tactics`) so `/assets/*` and `/pieces/*` remain real static files.

The same PR also fixed the Khmer check splash animation used by `ChessBoard`:

- Existing 1v1/local and online modes already showed the large Khmer `អុក` calligraphy splash when a king is in check.
- AI could immediately make its reply move and clear the check state, which caused the splash to disappear/cancel before it was visible.
- The splash logic was changed to be event-based so an AI response cannot immediately cancel the 3-second check animation.

The PR branch was verified with:

- `npm test`
- `npm run test:auth`
- `npm run check:assets`
- `npm run build`

The resulting `main` Vercel production deployment for commit `0dc7dda...` reached `READY`, and the build output contained hashed frontend assets including the mascot and app bundle.

### Manual verification still needed after PR #4

The next session should verify the actual production UI, not just the Vercel build state:

1. Open `https://ouk-khmer-online.vercel.app/` and confirm header mascot and Daily Wisdom mascot render instead of broken-image icons.
2. Enter Play vs AI and produce a check position. Confirm the large Khmer `អុក` splash appears over the board for the intended animation duration even if the AI replies quickly.
3. Confirm local 2-player and online still show the same splash behavior and were not regressed.
4. Confirm `/assets/...` and `/pieces/...` requests return their real files rather than `index.html`.

Do not claim the visual bug is fully closed until this production verification is done.

## Ranked persistence: important unresolved database-ID check

This is the highest-priority backend verification still outstanding.

The Firebase Firestore UI previously showed a UUID-like database selector / AI Studio breadcrumb rather than clearly showing `(default)`. Existing `/users` data is visible in that database.

Before treating server-authoritative ranking as fully production-verified, inspect both sides:

- Search frontend Firebase initialization for `getFirestore(` and determine whether it supplies a named database ID.
- Search repo for `ai-studio`, UUID-like database IDs, and Firestore database configuration.
- Inspect `server/ranked-manager.ts` Firestore REST URLs and determine whether they hardcode `(default)`.

If the frontend/data uses a named Firestore database while the backend writes `(default)`, ranked writes may go to the wrong database or fail. Do not assume the database is `(default)` based only on project ID.

After resolving that question, run one real two-account human-vs-human match and verify:

- winner rating/wins/gamesPlayed update once;
- loser rating/losses/gamesPlayed update once;
- a single matching `match_history` document exists;
- bot/AI games do not modify ranked stats.

## Firestore rules currently deployed

The deployed/source rule intent is:

- `/users/{uid}` remains public-readable for existing leaderboard compatibility.
- owners may create/update their own profile but cannot change ranked fields (`rating`, `peakRating`, `wins`, `losses`, `draws`, `winStreak`, `gamesPlayed`).
- `match_history` can be read by authenticated users but cannot be written by clients.
- everything else is denied by default.

The public/private profile split is intentionally deferred because the current leaderboard reads `/users` directly. Do not mix that migration into unrelated bug fixes.

## Domain / DNSSEC status

TenTen DNS is currently configured with:

- host: `ouk`
- type: `CNAME`
- target: `cname.vercel-dns.com`

Keep that CNAME. The Vercel certificate failure was not caused by the CNAME itself.

Vercel reported DNSSEC validation failure for `ouk.kuonkhmer.com`, specifically `DNSKEY Missing` / invalid DNSSEC delegation for `kuonkhmer.com`. The user is waiting for TenTen support to remove the stale/invalid DS state at the `.com` registry.

Suggested support wording already given to TenTen:

> kuonkhmer.com hiện có DNSSEC delegation/DS không hợp lệ. Public validation báo DNSKEY Missing mặc dù tôi không sử dụng DNSSEC. Vui lòng xóa toàn bộ DS record của kuonkhmer.com tại .COM registry, không phải kích hoạt dịch vụ DNSSEC.

Do not repeatedly remove/re-add the Vercel domain and do not tell the user to change the `ouk -> cname.vercel-dns.com` record while the DNSSEC issue is unresolved.

If moving DNS to Cloudflare later, remove/fix the stale registrar DS first, then migrate nameservers, create the `ouk` CNAME, and only enable DNSSEC after Cloudflare is active and the new DS can be published correctly.

## Intentionally unchanged product behavior

These are product choices, not bugs:

- displayed online count = real count + 50;
- AI fallback matchmaking;
- human-like bot names and randomized ratings;
- Folk / International / Blitz behavior;
- existing server-authoritative gameplay/clock/AFK engine.

Do not remove or "fix" these unless explicitly requested.

## Lower-priority technical debt

Still open but not urgent compared with production verification:

- event-level abuse/rate limiting;
- private-room PIN brute-force/rate limiting (PIN RNG itself is already secure);
- room/matchmaking state is in memory and is lost on backend restart;
- synchronous AI fallback can block the Node event loop under load;
- `/health` exposes detailed metrics;
- wildcard CORS is the fallback default if not configured;
- logger sanitization is shallow;
- package hygiene (`package-lock.json` plus `bun.lock`, large committed assets);
- public/private profile migration.

Prioritize simplicity and stability over adding infrastructure unless the issue is actually affecting production.

## How to continue in a fresh ChatGPT session

Tell the new session:

`Read docs/PROJECT_STATUS.md in machxanht/Ouk-Khmer-Online first, then continue from the Manual verification still needed after PR #4 section. Use the GitHub/Vercel connectors directly and do not ask me to repeat Railway or Firestore setup that is already marked DONE.`

Then, after asset/check-animation production verification, inspect the Firestore database-ID mismatch risk before doing more ranked work.
